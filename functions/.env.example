# Firebase Functions local/deployment configuration template.
# Do not commit real credentials or API keys.

COMPILER_API_URL=https://ce.judge0.com
COMPILER_API_TOKEN=
AZURE_SPEECH_KEY=
AZURE_SPEECH_REGION=eastus

# ZeptoMail is configured separately as the ZEPTOMAIL_CONFIG Secret.
# Example secret shape:
# {"apiKey":"YOUR_SEND_MAIL_TOKEN","fromEmail":"noreply@example.org","fromName":"FXEC Assessment Portal"}
